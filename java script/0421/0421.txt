제어판
	*중첩 => 알고리즘
	조건에 의한 분기
		*필터 역할
		*비교연산자/ 논리연산자
		if ~ else~
		if ~ else if ~ else ~
		switch ~ case ~ default ~
		삼항연산자
	조건에 의한 반복
		*초기식 / 조건식 / 증감식 => 횟수
		for
			for in
		while
		do ~ while
	* break, continue
* array
	- 하나의 변수에 여러개의 데이터(*)
	- 여러개의 변수의 묶음
	
	1차원 배열 선언
	let 배열명 = [];
	let 배열명 = Array();
	let 배열명 = new Array();
	
함수(function) -> 수학
	사용자 정의 함수
		함수의 선언
			변수/상수
			제어문
		함수의 호출
	내장함수
		-ECMAScript 제공하는 함수
함수 선언
	function 함수명(){
		함수 내용
			변수/ 상수
			제어문
	}	
함수 호출
	함수명()
	여러번 와도 상관 x









